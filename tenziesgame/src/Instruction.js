import React from "react"

function Instruction()
{
    return(<div className="Instruction">
        <h1>Tenzies</h1>
        <p>Roll until all dices are same.Click each dice to freeze at its current values between rolls</p>
       
    

    </div>)
}

export default Instruction;